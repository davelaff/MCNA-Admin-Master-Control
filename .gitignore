# Agent handoff snapshots (ephemeral session state)
HANDOFF.md

# Secrets and credentials
.env
*.env
*.pem
*.pfx
*.p12

# MSAL token caches (stored outside this folder, but belt-and-suspenders)
*.json.bak
.msal_*

# Claude Code local settings
.claude/settings.local.json

# Binary/Office files
*.docx
*.xlsx
*.pdf

# Windows
desktop.ini
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.pytest-basetemp/
.pytest-tmp/
mcp-server/.pytest-tmp/
.tmp-pytest/
.tmp-pytest-current/
.venv/
venv/

# SQLite KB — preserved via OneDrive sync, not git
mcp-server/kb/*.db
mcp-server/kb/*.db-shm
mcp-server/kb/*.db-wal
